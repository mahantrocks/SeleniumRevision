Selenium is a web application automation tool.

Architechture of Selenium :

Search context is a super most interface in selenium which extended by WebDriver, JavaScriptExecutor and TakesScreenshot interfaces

WebDriver interface implemented by RemoteWebDriver class.
RemoteWebDriver class extended by ChromDriver and FirefoxDriver class

Pending list for seleniun :
JavaScriptExecutor :

Git commands :

git init - TO initialize the local project to the remote repository
git add  
git commit -m"add project"
git remote add SeleniumRevision https://github.com/mahantrocks/SeleniumRevision.git
git push SeleniumRevision master

git reset --soft Head : undo latest commit

ls - to check list of directories in the folder

